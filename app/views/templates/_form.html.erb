<%= simple_form_for(@template) do |f| %>
  <%= f.error_notification %>

  <div class="inputs">
    <%= f.input :name %>
    <%= f.input :category %>
    <%= f.input :level %>
    <%= f.input :message %>
  </div>

  <div class="actions">
    <%= f.button :submit %>
  </div>
<% end %>
